---
status: draft
owner: TBD
last_reviewed: 2026-06-05
scope: Bug ticket → Support 描述 + 技术分析，分两层填写，供研发确认 + AI 修复使用
update_required_when:
  - 调整 Support 或技术分析字段
  - 新增 Bug 分类或严重等级定义
  - 修改 AI 提示词模板
---

# Bug 技术说明模板

本模板将 Bug 处理流程拆为两层：

- **Support 层**：从 ticket 沟通中提取，描述"用户看到了什么"。由 Support 填写（或 AI 辅助提取）。
- **技术分析层**：研发定位根因、评估影响、给出修复方案。由研发/技术负责人填写。

两层各自独立，按流程衔接：

```text
Ticket 沟通 → [AI 提取] → Support 层草案 → Support 确认
                                                    ↓
                                    研发接手 → 技术分析层填写 → AI 修复
```

---

# Part A — Support 层（由 Support 填写）

以下内容从 ticket 沟通中提取，关注用户视角的现象和影响，**不涉及技术根因**。

## A1. Bug 概述

| 字段 | 内容 |
| --- | --- |
| **Ticket 编号** | `<Support 系统 ID>` |
| **Bug 标题** | `<一句话描述用户看到的现象>` |
| **严重等级** | P0（阻断业务） / P1（核心流程异常） / P2（非核心功能异常） / P3（展示问题） |
| **提出方** | `<客户名称 / 内部团队>` |
| **影响用户** | `<哪些角色、多少人受影响>` |
| **发现时间** | `<YYYY-MM-DD HH:mm>` |
| **发现环境** | 生产 / 预发布 / 测试 |
| **复现频率** | 必现 / 偶现（约 X%） / 特定条件下必现 |

## A2. 现象描述

> 从用户视角描述，只写"看到什么"和"期望看到什么"。不写技术原因。

**实际行为**：
```
TODO: 用户看到了什么？附截图、错误提示原文、异常数据截图等。
```

**期望行为**：
```
TODO: 用户期望看到什么？正常流程应该是怎样的？
```

## A3. 复现步骤

| 步骤 | 操作 | 预期 | 实际 |
| --- | --- | --- | --- |
| 1 | `<例如：打开销售订单 SO/2024/001>` | 正常显示 | 正常 |
| 2 | `<点击"确认"按钮>` | 订单状态变为"已确认" | 报错：`<粘贴完整错误信息>` |
| 3 | ... | ... | ... |

**复现前提条件**（如有）：
```
TODO: 需特定权限？特定数据状态？特定浏览器/设备？特定配置？
```

## A4. 初步影响范围（用户视角）

| 维度 | 描述 |
| --- | --- |
| **受影响业务流程** | `<下单 / 发货 / 开票 / 付款 / 报表...>` |
| **受影响单据** | `<列出已知受影响的单据号或范围>` |
| **是否影响客户侧** | 是（门户/邮件/接口） / 否（仅内部） |
| **是否有临时绕过方式** | 是：`<描述>` / 否 |

## A5. 附加材料

- [ ] 截图/录屏：`<链接>`
- [ ] 错误日志：`<粘贴或链接>`
- [ ] 相关单据链接：`<Odoo 单据 URL>`
- [ ] 其他：`<描述>`

---

# Part B — 技术分析层（由研发/技术负责人填写）

以下内容在 Support 层确认后，由研发定位填写。**AI 可用于辅助分析，但最终由研发确认**。

## B1. 受影响对象（精确）

| 对象 | 类型 | 影响方式 | 对应 lifecycle 文档 |
| --- | --- | --- | --- |
| `<sale.order>` | P0 核心 | `<状态异常 / 数据错误 / 下游单据未生成>` | `docs/ai-dev/lifecycle/sale.order.md` |
| `<stock.picking>` | P0 核心 | `<未创建 / 数量错误>` | `docs/ai-dev/lifecycle/stock.picking.md` |
| ... | ... | ... | ... |

## B2. 根因分析

| 字段 | 内容 |
| --- | --- |
| **根因模块** | `<Tenways/tenways_xxx / odoo-17/addons/xxx / Tenways/vendor/xxx>` |
| **根因文件** | `<文件路径:行号>` |
| **根因方法** | `<方法名>` |
| **根因分类** | 逻辑错误 / 边界未处理 / 覆盖链冲突 / 配置错误 / 数据异常 / 第三方模块问题 |
| **根因描述** | `<为什么会触发这个 Bug？触发条件是什么？>` |
| **引入版本** | `<版本号 / commit hash / 上线日期>` |
| **引入原因** | `<需求变更遗漏 / 重构副作用 / 第三方模块升级 / 人工误操作>` |

## B3. 影响分析（技术视角）

| 维度 | 分析 |
| --- | --- |
| **已影响数据量** | `<估计已产生的异常记录数，SQL 查询结果>` |
| **下游影响** | `<是否影响下游单据生成、报表数据、外部系统数据>` |
| **运行时影响** | `<是否触发错误的 server action / automation / cron / mail template>` |
| **权限影响** | `<是否暴露越权数据、Portal 可见性问题>` |
| **性能影响** | `<是否造成慢查询、锁等待、超时>` |
| **回滚可行性** | `<纯代码修复即可 / 需数据修复跟进 / 需外部系统同步修正>` |

## B4. 修复方案

| 字段 | 内容 |
| --- | --- |
| **修复位置** | `<文件:行号 / 方法>` |
| **修复方式** | 新增校验 / 修改逻辑 / 补充边界 / 调整 super 顺序 / adapter 隔离 / 配置修正 |
| **涉及 Hook** | `<write / action_confirm / button_validate / _prepare_* / ...>` |
| **super 位置** | before / after / around / 不涉及 |
| **是否改变生命周期** | 是（说明哪个节点） / 否 |
| **是否影响旧行为** | 是（列出场景） / 否 |
| **是否需要数据修复** | 是（见 B5） / 否 |

**修复方案概要**：
```
TODO: 简述修复逻辑，不需要完整代码。说明为什么这样修、不那样修。
```

## B5. 数据修复（如需要）

| 字段 | 内容 |
| --- | --- |
| **修复范围** | `<哪些记录？筛选 SQL 条件>` |
| **修复方式** | SQL 脚本 / Python 脚本 / 手动逐条 |
| **执行环境** | 生产直接执行 / 需停机窗口 / 先预发布验证 |
| **修复前备份** | `<备份表名 / 导出文件路径>` |
| **回滚方案** | `<如修复失败，如何回滚数据>` |
| **执行人** | TBD |
| **预计影响时间** | `<分钟>` |

## B6. 测试验证

| 编号 | 场景 | 操作 | 预期结果 | 测试环境 |
| --- | --- | --- | --- | --- |
| T-01 | 原 Bug 复现场景 | `<按 A3 复现步骤>` | Bug 不再出现 | 预发布 |
| T-02 | 边界场景 | `<边界数据/并发/空值>` | 行为正确 | 预发布 |
| T-03 | 回归：关联流程 | `<相关正常操作>` | 不受影响 | 预发布 |
| T-04 | 回归：下游单据 | `<下游单据生成>` | 正常 | 预发布 |
| T-05 | 数据修复验证 | `<抽查修复后数据>` | 数据正确 | 预发布 |

## B7. 需要更新的治理文档

- [ ] `docs/ai-dev/lifecycle/{model}.md` — 如涉及生命周期变更
- [ ] `docs/ai-dev/runtime/` — 如涉及 server action / automation / cron
- [ ] `docs/ai-dev/rules/` — 如发现新的覆盖链冲突或规则
- [ ] `docs/ai-dev/vendor/` — 如涉及第三方模块修改

---

# AI 提示词

以下提供两套独立的提示词：

- **Prompt A**：从 ticket 沟通记录 → 生成 Support 层（Part A）
- **Prompt B**：从 Support 层 + 代码上下文 → 辅助生成技术分析层（Part B，研发最终确认）

---

## Prompt A：Ticket → Support 层

将 ticket 沟通记录输入 AI，自动生成 Part A 草案。使用时将 `{TICKET_CONTENT}` 替换为 ticket 沟通正文。

```text
你是一名 Odoo Support 工程师。请根据以下 ticket 沟通记录，提取并整理 Bug 的 Support 层信息。

## 背景
本项目是 Odoo 17 定制化项目，核心业务模块包括销售、库存、财务、采购等。

## 输出要求
请按以下结构输出，只关注用户视角的现象和影响，**不要分析技术根因**。
每个字段都必须填写，ticket 中缺失的信息标注"待补充"。

### A1. Bug 概述
- Ticket 编号、标题（一句话描述现象）
- 严重等级：P0（阻断业务）/ P1（核心流程异常）/ P2（非核心功能异常）/ P3（展示问题）
- 提出方、影响用户、发现时间、发现环境、复现频率

### A2. 现象描述
- 实际行为：用户看到了什么？粘贴错误提示原文。
- 期望行为：用户期望看到什么？

### A3. 复现步骤
- 按步骤编号列出操作路径，每步标注预期和实际结果
- 如有复现前提条件（权限/数据/配置/浏览器），单独列出

### A4. 初步影响范围（用户视角）
- 受影响业务流程、受影响单据、是否影响客户侧、是否有临时绕过方式

### A5. 附加材料
- 标注 ticket 中已有和缺失的材料（截图/日志/单据链接）

## 关键原则
- 只从 ticket 中提取，不要推测。
- 缺失信息标注"待补充"，并说明对定位的影响。
- 严重等级按实际业务影响判断，不要过度升级。
- 不要写技术原因、代码位置或修复建议。

## Ticket 沟通记录

{TICKET_CONTENT}
```

---

## Prompt B：Support 层 + 代码上下文 → 技术分析层

将 Support 层（Part A 已确认的内容）输入 AI，辅助生成 Part B 技术分析草案。**最终由研发确认**。
使用时将 `{SUPPORT_LAYER}` 替换为已确认的 Part A 内容，将 `{CODE_CONTEXT}` 替换为相关模块代码或 lifecycle 文档。

```text
你是一名 Odoo 技术研发工程师。请根据以下 Support 层 Bug 描述和代码上下文，生成技术分析层草案。

## 背景
本项目是 Odoo 17 定制化项目，包含自研模块（Tenways/）、Odoo 标准模块（odoo-17/）和第三方购买模块（Tenways/vendor/）。
核心规则：
- 默认不修改 odoo-17/ 标准模块和第三方购买模块。
- P0 核心对象修改需标注生命周期节点。
- 不确定的地方标注"待确认"。

## 输出要求
请按以下结构输出。无法从现有信息确定的字段标注"待确认"或"需研发定位"。

### B1. 受影响对象（精确）
- 列出受影响的核心对象，标注类型和影响方式
- 引用对应的 lifecycle 文档路径

### B2. 根因分析
- 给出初步怀疑方向：哪个模块、哪种类型的问题（逻辑错误/边界未处理/覆盖链冲突/配置/数据）
- 标注"需研发定位确认"，不要写确切文件路径和行号

### B3. 影响分析（技术视角）
- 已影响数据量、下游影响、运行时影响、权限影响
- 不确定的量级标注"需查询确认"

### B4. 修复方案（草案）
- 建议修复方式和涉及的 hook
- 标注是否影响旧行为、是否需要数据修复
- 给出修复方案概要（为什么这样修）

### B5. 数据修复（如需要）
- 如现象涉及异常数据，给出修复范围建议
- 标注修复方式、是否需要停机

### B6. 测试验证
- 给出至少 4 个测试场景：原 Bug 复现、边界、回归×2

### B7. 需要更新的治理文档
- 根据受影响对象列出 docs/ai-dev/ 下需要更新的文件

## 关键原则
- 不要猜测确切的根因代码行号，只给怀疑方向和模块级定位。
- 不确定的地方必须标注"待确认"或"需研发定位"。
- 修复方案只是建议草案，不执行，由研发确认后再开发。

## Support 层描述

{SUPPORT_LAYER}

## 代码上下文（可选）

{CODE_CONTEXT}
```

---

# 配合使用

技术分析层确认后，可将 B2（根因）+ B4（修复方案）+ B6（测试）作为 AI 修复任务输入：

```text
Bug 修复任务：
- Bug 描述：<A2 现象描述>
- 根因模块/分类：<B2 根因分析>
- 修复方式：<B4 修复方式>
- 涉及 Hook：<B4 涉及 Hook>
- 是否影响旧行为：<B4 是否影响旧行为>
- 测试场景：<B6 测试验证>
- 需要更新的治理文档：<B7>
```

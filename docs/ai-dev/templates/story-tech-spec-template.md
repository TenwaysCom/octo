---
status: draft
owner: TBD
last_reviewed: 2026-06-05
scope: Story 业务需求 → 简化版（需求确认）+ 技术说明版（研发后记录），两层分离
update_required_when:
  - 调整 Story 简化版或技术说明版字段
  - 新增跨模块、跨实例或集成注意事项
  - 修改 AI 提示词模板
---

# Story 技术说明模板

本模板将 Story 处理流程拆为两层：

- **简化版（需求确认层）**：从 PRD 提取核心信息 + 潜在风险分析，用于传达需求、对齐理解、识别风险。
- **技术说明版（实现记录层）**：研发完成后记录实际实现细节，用于知识沉淀、代码审查、后续 AI 维护。

两层各自独立，按流程衔接：

```text
PRD → [AI 提取] → 简化版草案 → 研发评审确认需求
                                        ↓
                                  开发实现
                                        ↓
                              [AI 辅助] → 技术说明版填写 → 归档 / PR 说明
```

---

# Part A — 简化版（需求确认层）

以下内容从 PRD 提取，用于**确认需求、对齐理解、预判风险**。不涉及具体实现细节。

## A1. 需求概述

| 字段 | 内容 |
| --- | --- |
| **PRD 链接** | `<PRD 文档链接>` |
| **Story 编号** | `<Meegle/Jira/TAPD ID>` |
| **需求标题** | `<一句话描述>` |
| **需求类型** | 新功能 / 功能增强 / 重构 / 集成对接 |
| **优先级** | P0 / P1 / P2 |
| **提出方** | `<产品/运营/客户>` |
| **目标上线** | `<版本或日期>` |

## A2. 业务背景

> 为什么需要这个功能？解决什么业务痛点？当前流程缺少什么？

```
TODO: 从 PRD 中提取业务背景，2-5 句话即可。
```

## A3. 用户故事与验收条件

| 编号 | 用户故事 | 验收条件 | 优先级 |
| --- | --- | --- | --- |
| US-01 | 作为`<角色>`，我希望`<功能>`，以便`<目的>` | `<可验证条件>` | P0 |
| US-02 | ... | ... | P1 |

## A4. 主对象与生命周期（初步判断）

| 字段 | 内容 |
| --- | --- |
| **主对象** | `<sale.order / stock.picking / account.move / ...>` |
| **关联对象** | `<列出所有可能受影响的 P0/P1 对象>` |
| **可能介入的生命周期节点** | `<action_confirm / button_validate / action_post / write / ...>` |
| **已有 lifecycle 文档** | `docs/ai-dev/lifecycle/{model}.md`（若不存在需先补最小版） |

## A5. 潜在风险分析

> 研发评审时预判，不需要精确到代码行。标注风险等级和关注点。

| 风险类别 | 风险描述 | 等级 | 关注点 |
| --- | --- | --- | --- |
| 覆盖链冲突 | `<是否可能与现有模块 hook 冲突？>` | P0/P1/P2 | `<需要查 lifecycle map 的哪个节点>` |
| 第三方模块 | `<是否涉及 vendor 模块？>` | P0/P1/P2 | `<能否用 adapter 隔离？>` |
| 外部集成 | `<是否影响 Shopify / WMS / 银行 / EDI？>` | P0/P1/P2 | `<接口变更、幂等、重试>` |
| 性能 | `<是否有大批量操作或同步外部调用？>` | P0/P1/P2 | `<是否需要 cron 异步化？>` |
| 权限 | `<是否涉及新角色、Portal 可见性？>` | P0/P1/P2 | `<需要新增 group？>` |
| 数据迁移 | `<是否需要改已有数据？>` | P0/P1/P2 | `<迁移范围、可回滚？>` |

## A6. 待澄清问题

> 从 PRD 中发现的模糊点，需产品/需求方确认后再开发。

| 编号 | 问题 | 影响范围 | 状态 |
| --- | --- | --- | --- |
| Q-01 | `<例如：当 x 条件不满足时，是否阻断还是允许继续？>` | `<影响异常流程设计>` | 待确认 |
| Q-02 | ... | ... | 待确认 |

---

# Part B — 技术说明版（研发后记录）

以下内容在**研发实现完成后**填写，记录实际做了什么、怎么做的、为什么这样做。用于知识沉淀、Cr 参考和后续 AI 维护。

## B1. 实际涉及模块

| 模块路径 | 类型 | 实际修改 | 说明 |
| --- | --- | --- | --- |
| `Tenways/tenways_xxx/` | 自研 | 新增文件 / 修改文件 | `<改了什么，附关键文件路径>` |
| `Tenways/tenways_yyy/` | 自研 | 仅调用（未修改） | `<复用方式>` |
| `odoo-17/addons/xxx/` | Odoo 标准 | 继承覆盖 / **未修改** | `<如继承覆盖，说明原因和覆盖的方法>` |
| `Tenways/vendor/zzz/` | 第三方购买 | **未修改** / adapter 隔离 | `<如用 adapter，说明 adapter 模块>` |

## B2. 实际涉及 Hook

| Hook 方法 | 所属模块 | super 位置 | 目的 | 阻断？ | 副作用 |
| --- | --- | --- | --- | --- | --- |
| `write()` | tenways_xxx | after | 校验字段变更 | 是 | 无 |
| `action_confirm()` | tenways_xxx | after | 触发下游单据 | 否 | 创建 stock.picking |
| ... | ... | ... | ... | ... | ... |

## B3. 正常流程（实际实现）

```
1. 用户触发 <入口>
2. 系统校验 <条件>
3. 执行 <核心逻辑>
4. 生成/更新 <下游对象>
5. 发送 <通知/邮件/接口回调>
```

## B4. 异常流程与边界

| 场景 | 实际行为 | 处理方式 |
| --- | --- | --- |
| input 不合法 | 阻断并提示 | 前端校验 + 后端 write 校验 |
| 下游单据已存在 | 跳过创建 | 幂等判断 |
| 外部接口超时 | 用户可继续操作，异步重试 | job queue + 状态标记 |
| 并发冲突 | 乐观锁 / user error | Odoo 默认 `write` 冲突机制 |

## B5. 规则与约束

- **不允许改变的旧行为**：`<列出保持不变的场景>`
- **Context flag**：`<列出依赖的 context key，例如 force_skip_xxx>`
- **权限影响**：`<是否新增 group、record rule、Portal 可见性变化>`
- **配置依赖**：`<是否依赖 ir.config_parameter、关键主数据>`
- **新增字段**：`<新增的模型字段，附字段名和用途>`

## B6. 运行时行为（实际新增/修改）

| 类型 | 名称/key | 说明 |
| --- | --- | --- |
| server action | `<action name>` | `<用途；若用代码替代则标注"不适用">` |
| base automation | `<rule name>` | `<用途；若用代码替代则标注"不适用">` |
| cron | `<cron name>` | `<用途、频率>` |
| mail template | `<template name>` | `<用途>` |
| QWeb report | `<report name>` | `<用途>` |

## B7. 外部集成（实际影响）

| 集成系统 | 接口/方式 | 实际影响 | 幂等 | 重试策略 | 失败处理 |
| --- | --- | --- | --- | --- | --- |
| `<Shopify / WMS / 银行 / EDI>` | `<REST / webhook / file>` | `<说明>` | 是/否 | `<策略>` | `<补偿>` |

## B8. 实现方案（最终选型）

> 简述最终选用的实现思路、关键决策及原因。这是后续维护和 AI 理解代码的关键上下文。

```
TODO:
- 为什么选择这个 hook 而不是别的？
- 为什么放在这个模块而不是别的？
- 有没有考虑过的备选方案？为什么放弃了？
- 有什么特别需要注意的细节（隐含假设、时序依赖等）？
```

## B9. 测试场景（实际执行）

| 编号 | 场景 | 覆盖节点 | 预期结果 | 测试结果 | 测试文件 |
| --- | --- | --- | --- | --- | --- |
| T-01 | 正常流程端到端 | action_confirm | `<预期>` | ✅ / ❌ | `<路径>` |
| T-02 | 边界：输入为空 | write | 阻断 + 提示 | ✅ / ❌ | `<路径>` |
| T-03 | 异常：外部接口超时 | post hook | 异步重试成功 | ✅ / ❌ | `<路径>` |
| T-04 | 回归：旧流程不受影响 | 旧 action | 行为不变 | ✅ / ❌ | `<路径>` |

## B10. 实际风险与依赖

| 风险 | 等级 | 实际应对 | 负责人 |
| --- | --- | --- | --- |
| `<风险描述>` | P0/P1/P2 | `<采取的措施>` | `<姓名>` |

## B11. 需要更新的治理文档

- [ ] `docs/ai-dev/lifecycle/{model}.md` — 新增/修改生命周期节点
- [ ] `docs/ai-dev/runtime/` — 新增 server action / cron / template
- [ ] `docs/ai-dev/vendor/` — 如有第三方模块变更
- [ ] `docs/ai-dev/rules/` — 如有新规则或约定

---

# AI 提示词

以下提供两套独立的提示词：

- **Prompt A**：PRD → 简化版（需求确认用）
- **Prompt B**：简化版 + 代码 diff → 技术说明版（研发后记录用）

---

## Prompt A：PRD → 简化版（需求确认）

将 PRD 文档输入 AI，生成简化版草案。使用时将 `{PRD_CONTENT}` 替换为 PRD 正文。

```text
你是一名 Odoo 技术项目经理。请根据以下 PRD 文档，生成一份简化的需求确认文档，用于研发评审。

## 背景
本项目是 Odoo 17 定制化项目，包含自研模块（Tenways/）、Odoo 标准模块（odoo-17/）和第三方购买模块（Tenways/vendor/）。
核心业务对象包括 sale.order、stock.picking、account.move、res.partner、product.product 等。

## 输出要求
请按以下结构输出，每个字段都必须填写（不确定的标注"待确认"）。

### A1. 需求概述
- 标题、类型（新功能/功能增强/重构/集成对接）、优先级（P0-P2）、提出方、目标上线

### A2. 业务背景
- 从 PRD 提取，2-5 句话说明为什么需要、解决什么痛点

### A3. 用户故事与验收条件
- 从 PRD 提取用户故事（格式：作为<角色>，我希望<功能>，以便<目的>）
- 每个故事配验收条件

### A4. 主对象与生命周期（初步判断）
- 主对象、关联对象
- 可能介入的生命周期节点（action_confirm / button_validate / action_post / write 等）
- 引用已有 lifecycle 文档路径

### A5. 潜在风险分析
- 覆盖链冲突、第三方模块、外部集成、性能、权限、数据迁移
- 每个风险标注等级和关注点，不确定的标注"待研发评估"

### A6. 待澄清问题
- 从 PRD 中发现的模糊点，列出问题、影响范围
- 标注"待产品确认"

## 关键原则
- 只描述"需要做什么"，不写"怎么做"（实现方案留给研发）。
- 风险分析是预判，不是结论。
- 默认不动 odoo-17/ 标准模块和第三方购买模块。
- 涉及 P0 核心对象必须标注生命周期节点。
- 不确定的地方标注"待确认"或"待澄清"。

## PRD 文档内容

{PRD_CONTENT}
```

---

## Prompt B：简化版 + 代码 → 技术说明版（研发后记录）

将已确认的简化版 + 实现的代码 diff 输入 AI，辅助生成技术说明版。使用时将 `{SIMPLIFIED_SPEC}` 替换为已确认的 Part A 内容，将 `{CODE_DIFF}` 替换为 git diff 或关键文件变更说明。

```text
你是一名 Odoo 技术研发工程师。请根据以下需求简化版和代码变更，生成技术说明文档，用于知识沉淀和后续 AI 维护。

## 背景
本项目是 Odoo 17 定制化项目，包含自研模块（Tenways/）、Odoo 标准模块（odoo-17/）和第三方购买模块（Tenways/vendor/）。
核心规则：
- 默认不修改 odoo-17/ 标准模块和第三方购买模块。
- P0 核心对象修改需标注生命周期节点。

## 输出要求
请按以下结构输出。无法确定的信息标注"待补充"。

### B1. 实际涉及模块
- 列出实际修改的模块路径、文件、修改性质
- 区分"修改"和"仅调用"

### B2. 实际涉及 Hook
- 每个 hook 的方法名、模块、super 位置（before/after/around）
- 目的、是否阻断、副作用

### B3. 正常流程（实际实现）
- 描述实现后的实际流程步骤

### B4. 异常流程与边界
- 列出实际处理的异常场景和处理方式

### B5. 规则与约束
- 不允许改变的旧行为、context flag、权限影响、配置依赖、新增字段

### B6. 运行时行为
- 是否新增 server action / automation / cron / mail template / report

### B7. 外部集成
- 是否影响外部系统，实际影响描述

### B8. 实现方案（重点）
- 为什么选择这个 hook 而不是别的？
- 为什么放在这个模块而不是别的？
- 有没有备选方案？为什么放弃？
- 特别需要注意的细节

### B9. 测试场景
- 列出正常、边界、异常、回归的测试场景和结果

### B10. 实际风险与应对
- 标注实际遇到的风险和采取的措施

### B11. 需要更新的治理文档
- 列出 docs/ai-dev/ 下需要更新的文件

## 关键原则
- 基于实际代码变更描述，不要编造。
- 实现方案部分是给后续维护者的上下文，讲清楚"为什么这样做"。
- 不确定的地方标注"待补充"。

## 需求简化版

{SIMPLIFIED_SPEC}

## 代码变更

{CODE_DIFF}
```

---

# 配合使用

技术说明版完成后，可将关键字段映射到 `ai-task-template.md` 或 PR 说明中：

```text
业务目标：<A1 需求标题 + A2 业务背景>
主对象：<A4 主对象>
关联对象：<A4 关联对象>
生命周期节点：<B1/B2 涉及节点>
触发入口：<B3 正常流程>
涉及 hook：<B2 实际涉及 Hook>
已有扩展模块：<B1 实际涉及模块>
重复覆盖或交叉覆盖：<A5 风险 / B10 实际风险>
super 调用位置：<B2 super 位置>
context flag：<B5 Context flag>
正常流程：<B3>
异常流程：<B4>
不允许改变的旧行为：<B5>
运行时配置或数据库自动化：<B6>
第三方模块：<B1>
外部集成：<B7>
推荐测试：<B9>
需要更新的治理文档：<B11>
```

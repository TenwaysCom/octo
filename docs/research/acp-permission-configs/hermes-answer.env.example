# 仅供 Answer 任务的 Hermes 子进程使用；__ACTION_DIR__ 必须替换。
# 本机 Hermes 只支持单个 safe root，不支持用冒号连接多个目录。
# 只约束原生文件写工具，不约束终端/脚本间接写入。
HERMES_YOLO_MODE=0
HERMES_WRITE_SAFE_ROOT=__ACTION_DIR__

# 现有 fetch 脚本将证据快照写入同一个任务临时目录。
OCTO_SUPPORT_QA_ACTION_DIR=__ACTION_DIR__

# Document 的多个可写位置不能用本机单根配置准确表达；见 README.md。

{
  "name": "tenways-octo-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && npm run copy-assets && npm run validate",
    "copy-assets": "cp src/popup.html src/popup.js dist/ && cp manifest.json dist/ && cp -r icons dist/",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "validate": "node scripts/validate.mjs",
    "package": "npm run clean && npm run build && cd dist && zip -r ../tenways-octo-extension.zip .",
    "test": "vitest run"
  },
  "dependencies": {
    "webextension-polyfill": "^0.10.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "@types/node": "^24.12.0",
    "@types/webextension-polyfill": "^0.10.7",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
